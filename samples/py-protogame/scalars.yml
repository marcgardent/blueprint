
definitions:
    String:
        binding:
            python:
                factory: str
                package: builtin
                reference: str
        inputs:
            object : string
        outputs:
            self: string
    List:
        binding:
            python:
                factory: list
                package: builtin
                reference: list
        inputs:
            content : list(*)
        outputs:
            self: list(*)