
definitions:
    #########################
    String:
        #########################
            binding: "str"
            inputs:
                text : string
            outputs:
                self: string